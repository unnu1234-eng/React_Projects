import React, {Component} from 'react'

export default class Propcomponent extends Component [
    render() {
        return (
            <div>
            <h2> And now I am teaching in { this.props.section}</h2>
            </div>
        )
    }
]